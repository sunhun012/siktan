# 오늘의 식판 (급식표 웹앱)

교육부 나이스(NEIS) 오픈API에서 실제 학교 급식을 가져와 보여주는 PWA. 사용자는 한국어만 쓰며,
코드를 직접 읽지 않는다. **설명과 화면 문구는 모두 한국어로**, 전문용어 없이 쓴다.

## 지금 상태 (2026-08-17 기준)

| | |
|---|---|
| 라이브 | https://siktan.netlify.app |
| 저장소 | https://github.com/sunhun012/siktan (main 브랜치) |
| 호스팅 | Netlify 프로젝트 `siktan`, GitHub 연결 → **push하면 자동 배포** (보통 20초 내) |
| 소스 | `~/Desktop/김재인 클로드/급식표/` |

완료된 것: 학교 검색·저장, 오늘/다음 급식 카드, 주간·월간 보기, 조식/중식/석식, 칼로리·영양·원산지,
알레르기 번호 표시, 다크모드, 인쇄, 홈 화면 앱 설치(PWA), 오프라인 열람, 의견 게시판(자기 글 보기 +
관리자 답장·삭제).

## 구조

```
web/                    ← Netlify publish directory (netlify.toml에 지정)
  index.html            ← 앱 전체. 화면·스타일·기능이 이 한 파일에 다 있다
  manifest.webmanifest  ← 홈 화면 앱 정보
  sw.js                 ← 서비스워커 (오프라인)
  _headers              ← 매니페스트 MIME 교정
  icon-*.png            ← 아이콘 (tools 없이 PIL로 생성했음)
netlify/functions/
  feedback.mjs          ← 의견 게시판 API, 주소는 /api/feedback
tools/
  test.sh, harness.js, fixture.json   ← 아래 '테스트' 참고
급식표 열기.command       ← 맥에서 로컬 서버로 여는 실행 파일
```

## 테스트

이 맥에는 **node·npm이 없다.** 브라우저 화면도 캡처할 수 없다(권한 없음). 대신 macOS 내장
JavaScriptCore로 앱을 가짜 DOM에 올려 돌린다. 실제 급식 데이터(`fixture.json`)와 게시판 API를
흉내내고, 화면에 그려진 결과를 글자로 확인한다.

```bash
./tools/test.sh
```

**코드를 고쳤으면 배포 전에 반드시 이걸 돌린다.** 지금까지 이 하네스가 잡아낸 실제 버그:
빈 구간까지 재귀 분할해 요청이 3배로 늘던 문제, 방금 남긴 글이 목록에 안 뜨던 문제.

새 UI 요소를 추가하면 `harness.js`의 `IDS` 배열에 해당 id를 넣어야 한다. 하네스에 없는 브라우저
API를 쓰면(예: `navigator`, `dialog.open`) 스텁도 함께 채워야 한다.

## 배포

```bash
git add -A && git commit -m "..." && git push origin main
```
20초쯤 뒤 반영된다. 확인:
```bash
curl -s https://siktan.netlify.app/ | grep -o '<title>[^<]*</title>'
```
**Netlify CLI는 없다** (npm이 없어 설치 불가). 드래그 배포는 이제 쓰지 않는다.

## 반드시 알아야 할 함정

1. **나이스는 인증키 없이 5건만 준다.** `pIndex` 페이징도 무시된다. 그래서 `fetchMeals()`가
   `list_total_count`(잘리기 전 실제 건수)를 보고 구간을 반으로 쪼개 재귀 조회한다.
   급식 없는 구간은 `INFO-200`으로 오는데, 이때 총 건수를 0으로 잡아주지 않으면 하루 단위까지
   쪼개며 요청이 폭증한다 (`withTotal` 함수가 그 역할).

2. **Netlify Blobs는 기본이 느슨한 일관성.** 방금 쓴 값을 바로 읽으면 없는 것으로 나온다.
   `getStore({ name, consistency: 'strong' })`로 열어야 한다. 화면 쪽도 서버 재조회를 기다리지 않고
   방금 남긴 글을 목록에 바로 넣는다.

3. **서비스워커가 `/api/`를 캐시하면 안 된다.** 남이 방금 쓴 글이 안 보이게 된다. `sw.js`에서 제외 중.
   `sw.js`를 고치면 `CACHE` 버전 문자열(`siktan-vN`)을 올려야 기존 사용자에게 반영된다.

4. **`<meta charset="utf-8">`은 필수.** 빠뜨리면 로컬 서버로 열 때 한글이 깨진다(실제로 겪었음).

5. **Safari는 `file://`에서 https fetch를 막는다.** 로컬 확인은 `급식표 열기.command`로 서버를 띄워서.

## 의견 게시판

- 누구나 글을 남긴다. 글쓴이는 **자기 글과 거기 달린 답장만** 본다.
- 계정은 없다. 기기마다 만든 무작위 표식(`meal.who`)을 글에 붙여 그걸로 찾는다.
  → 기기가 바뀌거나 브라우저 데이터를 지우면 이전 글을 못 찾는다. 알려진 한계.
- 관리자는 전체를 보고 답장·삭제한다. 판정은 **서버에서** 한다 (화면에서만 숨기면 주소로 다 읽힌다).
- 관리자 = Netlify 환경변수 `ADMIN_KEY` 값을 아는 사람. 앱 설정에 그 값을 넣으면 그 기기가 관리자가 된다.
  사용자가 직접 정한 값이며 **나는 그 값을 모른다.** 환경변수를 바꾸면 재배포해야 적용된다.
- 지금 서버에 확인용으로 남긴 테스트 글이 몇 개 있다. 사용자가 관리자로 들어가 지울 수 있다.

## 이야기가 나왔지만 아직 안 한 것

- 아침에 오늘 급식을 알려주는 알림
- 여러 학교 저장해두고 전환하기
- 좋아하는 메뉴 나오는 날 표시
- 관리자 키를 여러 번 틀리면 잠시 막기 (현재 시도 횟수 제한 없음)
- 예전 드래그 배포 사이트 `thunderous-crisp-a13792.netlify.app` 삭제 (지금은 방치 상태)
